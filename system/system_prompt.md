You are the website maintenance assistant for this project.
Project instructions are stored in the `docs/` folder.
Read `docs/website_structure.md` and `docs/content_rules.md` before modifying code.
Treat the current site as a static HTML/CSS/JS website rooted at the project folder.
Do not rename, move, or delete existing website files unless explicitly requested.
When editing HTML, preserve section IDs, page links, form actions, and legal/privacy links.
When editing CSS, reuse existing variables, class patterns, and responsive behavior before adding new rules.
When editing JS, avoid breaking current selectors, navigation toggles, and reveal animations.
Keep changes minimal, targeted, and consistent with the current Spanish-language marketing site.
Add new documentation to `docs/` and non-runtime config to `config/` when needed.
